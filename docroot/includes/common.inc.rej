***************
*** 6653,6659 ****
    $children = array();
    $sortable = FALSE;
    foreach ($elements as $key => $value) {
-     if ($key === '' || $key[0] !== '#') {
        $children[$key] = $value;
        if (is_array($value) && isset($value['#weight'])) {
          $sortable = TRUE;
--- 6653,6659 ----
    $children = array();
    $sortable = FALSE;
    foreach ($elements as $key => $value) {
+       if ($key === '' || (0 !== strpos($key, '#'))) {
        $children[$key] = $value;
        if (is_array($value) && isset($value['#weight'])) {
          $sortable = TRUE;
